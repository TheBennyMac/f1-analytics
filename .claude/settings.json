{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'if echo \"$CLAUDE_TOOL_INPUT\" | grep -q \"git commit\"; then cd \"c:/Users/Benny Mac/f1-analytics\" && python -m pytest tests/ -q --tb=short 2>&1 | tail -20; fi'"
          }
        ]
      }
    ]
  }
}
